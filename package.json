{
  "name": "celestial-globe",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@react-three/fiber": "^9.1.0",
    "d3-geo": "^3.1.1",
    "d3-scale": "^4.0.2",
    "jest": "^29.7.0",
    "next": "^14.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "three": "^0.163.0",
    "zustand": "^4.5.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/react": "latest",
    "@types/three": "^0.160.0",
    "autoprefixer": "^10.4.21",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.1",
    "jest-environment-jsdom": "^29.7.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "AI Assistant Notes": [
    "This `package.json` is for a Next.js project visualizing celestial data on a 3D globe.",
    "The core rendering is done with Three.js and react-three-fiber.",
    "Data is loaded from a local JSON file (`public/celestial_data.json`).",
    "Tailwind CSS has been added to address the `@tailwindcss/postcss` dependency issue.",
    "The `download-data` script has been updated to use the latest planetary ephemeris kernel (de440.bsp).",
    "Consider suggesting additional dependencies if you detect missing functionality during code generation.",
    "Prioritize dependencies with active maintenance and good community support.",
    "When generating code, assume TypeScript is being used.",
    "If asked to add a feature, check if a suitable library already exists before writing custom code."
  ]
}
